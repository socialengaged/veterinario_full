# Usato automaticamente da `vite build`. URL pubblico API (HTTPS).
# Per sviluppo locale: usa `.env` con http://127.0.0.1:8060 (vedi .env.example).
VITE_API_BASE_URL=https://api.veterinariovicino.it
# Email destinatario PayPal (consulenza online — mostrata nel sito e nelle email)
VITE_ONLINE_CONSULT_PAYPAL_EMAIL=vet.stella@gmail.com
