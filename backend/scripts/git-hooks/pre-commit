#!/usr/bin/env bash
# Copiare in .git/hooks/pre-commit (vedi scripts/install_git_hooks.sh)
set -e

echo "Running basic checks..."

if git diff --cached --name-only | grep -qE '(^|/)\.env$'; then
  echo "ERROR: .env file cannot be committed"
  exit 1
fi

echo "OK"
